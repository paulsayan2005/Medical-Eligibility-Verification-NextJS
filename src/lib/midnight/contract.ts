// import { medical_eligibility_verification } from 'contract/src/managed/medical-eligibility-verification/contract';
// import type { MedicalEligibilityVerificationProviders } from 'contract/src/managed/medical-eligibility-verification/contract';

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const loadContract = async (providers: any, address: string) => {
  /*
  const contract = await medical_eligibility_verification.find(providers, address);
  return contract;
  */
  console.log("Loading contract at", address);
  return null;
};
